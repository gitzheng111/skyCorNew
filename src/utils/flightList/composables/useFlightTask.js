export function useFlightTask() {

    const taskNeedData = ref([])

    const taskList = ref([])

    const createTask = ()=>{}

    const submitTask = ()=>{}

    return {

        taskNeedData,

        taskList,

        createTask,

        submitTask
    }
}